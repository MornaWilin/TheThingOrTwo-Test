# TEST FOR THETHINGORTWO

* FrontEnd: React
* BackEnd: Node.js (Express, Axios)
* DB: Firebase Firestore (NoSQL) //i can provide a console if needed

# DOCKER COMPOSED

to run:

**docker-compose up --build**
